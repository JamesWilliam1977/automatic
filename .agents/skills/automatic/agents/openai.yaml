interface:
  display_name: "Automatic"
  short_description: "Repo-specific patterns and workflows for automatic"
  default_prompt: "Use the automatic repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true